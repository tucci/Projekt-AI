using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum WeaponEnum
{
    Hitscan = 0,
    RocketLauncher = 1,
    BeamWeapon = 2,
    QuickProjectileWeapon = 3,
    LobWeapon = 4,
    MUST_OVERRIDE = -1
}

public enum WeaponType
{
    Projectile = 10,
    LineRendererType = 20
}
